spring:
  application:
    name: backend-api
  datasource:
    url: jdbc:mysql://localhost:3306/hill_commerce
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

server:
  port: 8080
